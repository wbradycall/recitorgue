public class Regales extends Stop {
    private String reedType;
    private double scalingFactor;

    public Regales(String name, int pitch, int numberOfPipes, String reedType, double scalingFactor) {
        super(name, new Rank[]{new Rank(pitch, numberOfPipes)});
        this.reedType = reedType;
        this.scalingFactor = scalingFactor;
    }

    public String getReedType() {
        return reedType;
    }

    public void setReedType(String reedType) {
        this.reedType = reedType;
    }

    public double getScalingFactor() {
        return scalingFactor;
    }

    public void setScalingFactor(double scalingFactor) {
        this.scalingFactor = scalingFactor;
    }

    // Method to calculate scaled diameter based on scaling factor
    public double calculateScaledDiameter(double originalDiameter) {
        return originalDiameter * scalingFactor;
    }

    // Method to generate sound for Regales
    public void generateSound() {
        for (Rank rank : getRanks()) {
            int pitch = rank.getPitch();
            int numberOfPipes = rank.getNumberOfPipes();
            double originalDiameter = 16.0; // Assuming an original diameter of 16.0 for Regales
            double originalLength = 1.0; // Assuming original length of 1.0
            
            // Calculate scaled diameter and quarter-length based on scaling factor
            double scaledDiameter = calculateScaledDiameter(originalDiameter);
            double scaledLength = originalLength / 4; // Same quarter length as Zinkes for Regales
            
            // Implement sound generation logic for Regales with one end partially closed
            System.out.println("Sound for Regales stop with pitch " + pitch + " and scaled diameter " + scaledDiameter + " at quarter length with one end partially closed, generated by " + reedType + " reed.");
        }
    }
}
